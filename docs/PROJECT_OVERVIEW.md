# 🍙 源飯糰 AI 語音點餐系統 (AI Ordering System)

## 📋 專案簡介

這是一個**台灣便利餐飲連鎖店「源飯糰」**專用的 AI 語音點餐系統。系統能夠處理自然語言輸入（語音/文字），解析用戶訂單請求，管理購物車，處理結帳流程，並將訂單持久化到資料庫。

**核心特色：**
- 🎙️ 自然語言對話管理（slot-filling 技術）
- 🛒 智能購物車管理
- 📦 多品項訂單拆分與処理
- 💰 動態套餐飲料換購（價格驅動確認）
- 🔄 訂單編輯功能（移除、取消、清空）
- 💾 SQLite 訂單持久化
- 🛡️ 完整的安全性測試

---

## 📁 目錄結構

```
ai-ordering-system/
├── .claude/                          # Claude Code 配置
├── .github/
│   └── workflows/
│       └── tests.yml                # CI/CD GitHub Actions
├── .venv/                            # Python 虛擬環境
├── docs/                             # 文檔文件夾
├── prompts/                          # LLM 系統提示詞
├── scripts/                          # 工具腳本
├── src/                              # 主要源碼
│   ├── main.py                      # CLI 互動式入口
│   ├── run_dm_cli.py                # 對話管理器測試 CLI
│   ├── agents/                      # AI 代理（遺留）
│   ├── api/                         # FastAPI Web 服務
│   ├── config/                      # 配置加載器
│   ├── dm/                          # 對話管理器核心
│   ├── repository/                  # 資料庫/持久化層
│   ├── services/                    # 外部服務（LLM、ASR、TTS）
│   └── tools/                       # 產品特定解析工具
│       ├── menu/                    # 菜單數據與定價
│       ├── riceball_tool.py         # 飯糰解析
│       ├── drink_tool.py            # 飲品解析
│       ├── carrier_tool.py          # 漢堡/吐司解析
│       ├── combo_tool.py            # 套餐解析
│       ├── snack_tool.py            # 點心解析
│       ├── jam_toast_tool.py        # 醬料吐司
│       ├── egg_pancake_tool.py      # 蛋餅
│       └── order_router.py          # 意圖路由器
├── tests/                            # 測試套件
├── .env                              # 環境配置
├── .gitignore                        # Git 忽略規則
├── .python-version                   # Python 版本要求
├── pyproject.toml                    # 項目配置
├── README.md                         # 項目 README
├── uv.lock                           # 依賴鎖定文件
└── orders.db                         # SQLite 訂單資料庫
```

---

## 🏗️ 核心模組說明

### 1️⃣ **對話管理器** (`src/dm/dialogue_manager.py`)
主要的對話狀態機，控制整個對話流程。

**主要功能：**
- 🎯 **會話狀態管理**：維護每個會話的購物車、待處理項目、對話歷史
- 📝 **Slot-Filling**：當缺少商品屬性時進行澄清提問
  - 例：飯糰 → 缺少「米種」(白米/紫米/混合) → 提問用戶
- 📦 **多品項拆分**：按關鍵詞（、、跟、還要、再來）拆分語句
- 🔀 **訂單路由**：分類用戶輸入到商品類型或編輯操作
- 💳 **結帳流程**：驗證購物車、生成訂單摘要、確認後提交
- 🎁 **套餐處理**：特殊處理含飲料和點心的套餐
- 💰 **價格驅動確認**：換購更貴的飲料時提示漲價
- 🔒 **訂單凍結**：提交後防止修改

**會話狀態結構：**
```python
{
    "cart": [items],                 # 已確認商品
    "pending_frames": [frames],      # 待澄清的商品
    "history": [utterances],         # 對話歷史
    "status": "OPEN|CONFIRMING_CHECKOUT|SUBMITTED",
    "current_combo_frame": {},       # 當前正在組建的套餐
    "last_user_text": str,
    "order_payload": {}              # 最終訂單
}
```

### 2️⃣ **訂單路由器** (`src/tools/order_router.py`)
智能路由分類器，將用戶輸入分配到正確的工具。

**功能：**
- 📍 商品類型識別：飯糰、飲品、漢堡/吐司、點心、蛋餅、醬料吐司、套餐
- ✏️ 編輯操作偵測：結帳、取消、清空、移除
- 🔤 文本正規化：同音字修正
- ⚡ 優先級處理：多匹配時的優先選擇

### 3️⃣ **商品解析工具**

每個工具負責解析對應商品類別，提取屬性並計算價格：

| 工具 | 功能 | 關鍵屬性 |
|------|------|--------|
| **riceball_tool.py** | 飯糰解析 | 口味、米種(白/紫/混)、數量、加蛋/起司、食材增減 |
| **drink_tool.py** | 飲品解析 | 飲品名、大小(中/大)、溫度(冰/熱/溫)、糖度 |
| **carrier_tool.py** | 漢堡/吐司 | 種類(漢堡/吐司/饅頭)、口味、食材增減 |
| **combo_tool.py** | 套餐解析 | 套餐名、飲料換購、價格計算 |
| **snack_tool.py** | 點心解析 | 點心類型、數量(支/片) |
| **jam_toast_tool.py** | 醬料吐司 | 醬料、厚度、切割方式 |
| **egg_pancake_tool.py** | 蛋餅 | 口味、自訂選項 |

### 4️⃣ **菜單與定價** (`src/tools/menu/`)

- **menu_all.json**：完整菜單資料庫（所有商品、分類、價格）
- **menu_price_service.py**：菜單查詢服務
  - 動態價格查詢（按分類+商品名）
  - 支援大小變體：「(中)」、「(大)」
  - 數量倍增計算

### 5️⃣ **API 服務** (`src/api/app.py`)

FastAPI Web 服務，提供 REST API 介面。

**端點：**
- `GET /healthz` - 健康檢查
- `GET /orders/{order_id}` - 獲取單筆訂單
- `GET /orders` - 列表查詢訂單（支援日期/狀態篩選）
- 認證：HTTP Header `X-API-Key`

### 6️⃣ **資料持久化** (`src/repository/order_repository.py`)

SQLite 訂單儲存：
```sql
CREATE TABLE orders (
    order_id TEXT PRIMARY KEY,           -- "SN-0125-XXXX"
    status TEXT NOT NULL,                -- "SUCCESS", "SUBMITTED"
    created_at TEXT NOT NULL,            -- ISO 時間戳
    session_id TEXT NOT NULL,            -- 會話 ID
    items_json TEXT NOT NULL,            -- JSON 商品陣列
    total_price INTEGER NOT NULL,        -- 總價
    order_payload_json TEXT NOT NULL     -- 完整訂單
)
```

---

## 💬 對話流程範例

```
客戶: "我要一個薯餅和一杯豆漿"

【對話管理器】
1. 分割：「薯餅」、「豆漿」
2. 路由「薯餅」→ snack_tool → {snack: "薯餅(1片)", quantity: 1}
3. 路由「豆漿」→ drink_tool → {drink: "有糖豆漿", missing: ["temp", "size"]}
4. 添加至 pending_frames

系統: "你的豆漿要冰的還溫的？"
客戶: "大冰"

【drink_tool 解析】
→ size: "大杯", temp: "冰"
→ 完成飲品 frame，移至購物車

系統: "好的，薯餅、有糖豆漿(大杯, 冰)，還需要什麼嗎？"
客戶: "結帳"

【結帳流程】
系統: "這樣一共薯餅、有糖豆漿(大杯, 冰)，共 2 個品項，共 40元。確定要送出訂單嗎？"
客戶: "好"

→ 訂單送出 ✅
→ 生成訂單 ID：SN-0125-XXXX
→ 保存至 SQLite

系統: "好的，訂單已送出！您的訂單編號是 SN-0125-XXXX，請至櫃檯結帳領取。"
```

---

## 🛡️ 安全特性

- ✅ 負向測試防止注入攻誤觸發
- ✅ 套餐價格注入防護
- ✅ 訂單凍結（防修改已提交訂單）
- ✅ 數據驗證（Pydantic 模式）
- ✅ API 金鑰認證

---

## 🧪 測試架構

使用 **pytest** 加 BDD 標記組織：

```bash
# 所有測試
uv run pytest -q

# 行為驅動測試
uv run pytest -m bdd

# 安全性測試
uv run pytest -m security

# 契約測試
uv run pytest -m contract
```

**主要測試文件：**
- `test_dialogue_manager_bdd.py` - 對話流程
- `test_order_router_bdd.py` - 意圖路由
- `test_combo_integration.py` - 套餐訂單
- `test_checkout_submit.py` - 結帳提交
- `test_order_editing_cancel_remove_clear.py` - 購物車編輯
- `test_order_persistence_and_query_api.py` - 資料庫操作
- `test_combo_security.py` - 安全性防護

---

## ⚙️ 配置文件

| 文件 | 用途 |
|------|------|
| `.env` | LM Studio URL、ASR/TTS 配置 |
| `pyproject.toml` | 項目元數據、依賴、pytest 配置 |
| `src/config/combo_aliases.json` | 套餐名稱簡化規則 |
| `src/config/addon_prices.json` | 食材加價 |
| `src/tools/menu/menu_all.json` | 完整菜單 |
| `src/tools/menu/riceball_recipes.json` | 飯糰風味定義 |

---

## 📦 主要依賴

**核心：**
- `fastapi>=0.104.1` - Web 框架
- `uvicorn>=0.24.0` - ASGI 伺服器
- `openai>=1.3.0` - LLM 客戶端
- `pydantic>=2.5.0` - 資料驗證
- `requests>=2.32.5` - HTTP 客戶端

**開發：**
- `pytest>=7.4.0` - 測試框架
- `pytest-bdd>=8.1.0` - BDD 支援
- `black>=23.12.0` - 代碼格式化
- `mypy>=1.7.0` - 型別檢查
- `ruff>=0.1.13` - Linting

---

## 🚀 使用方式

### 啟動互動式 CLI
```bash
uv run python src/main.py
```

### 啟動對話管理器 CLI
```bash
uv run python src/run_dm_cli.py
```

### 啟動 FastAPI 服務
```bash
uv run uvicorn src.api.app:app --reload
```

### 執行測試
```bash
uv run pytest -q
```

---

## 📊 系統架構特點

| 特性 | 說明 |
|------|------|
| **架構** | 對話狀態機 + Slot-Filling |
| **NLP 方法** | 關鍵詞路由 + 規則型框架解析（無 ML 模型） |
| **可擴展性** | 每會話內存狀態（可升級到 Redis） |
| **定價** | 動態查詢 JSON 菜單服務 |
| **持久化** | SQLite 訂單歷史 |
| **API** | FastAPI + API Key 認證 |
| **LLM 支援** | LM Studio (OpenAI 相容 API) |
| **測試覆蓋** | BDD + 安全性 + 契約測試 |

---

## 📝 最近提交歷史

| 提交 | 說明 |
|------|------|
| `b5803a2` | Fix: 修正米漿別名映射為花生糙米漿，並新增混漿映射 |
| `5a619f7` | Feature: 新增價格驅動的套餐換飲料（米漿）流程 |
| `4da8b89` | Test: 新增套餐負向測試，防誤觸發與價格注入 |
| `b76105d` | Fix: 修正多品項 pending queue，統一使用 cart 並完善缺槽判定 |
| `d6a8038` | Fix: 修正補槽重複下單與路由判斷，新增果醬吐司工具 |

---

## 💾 當前狀態

**修改檔案（待提交）：**
- `src/dm/dialogue_manager.py` - 對話流程更新
- `src/tools/order_router.py` - 意圖路由改進

**未追蹤檔案：**
- `orders.db` - 實時資料庫
- `src/api/` - API 模組
- `src/repository/` - Repository 模組
- 測試資料庫與測試檔案

---

## 🔧 技術棧總結

| 層級 | 技術 |
|------|------|
| **後端框架** | FastAPI + Uvicorn |
| **對話引擎** | Custom DialogueManager (Slot-Filling) |
| **NLP** | 規則型解析 + 關鍵詞匹配 |
| **資料庫** | SQLite |
| **認證** | API Key (HTTP Header) |
| **LLM** | LM Studio OpenAI-compatible API |
| **測試** | pytest + pytest-bdd |
| **代碼質量** | black, ruff, mypy |
| **包管理** | uv (ultra-fast Python package manager) |

---

## 📄 文檔與資源

- 主要說明文檔：`README.md`
- 本文件：`PROJECT_OVERVIEW.md` (詳細項目概覽)
- 菜單定義：`src/tools/menu/menu_all.json`
- 測試文件：`tests/` 目錄

---

**版本：** 0.1.0
**許可證：** MIT
**Python 需求：** >=3.10
**最後更新：** 2026-01-25

